{% extends './base.njk' %}

{% block content %}
  <article class="content content--{{ page.url | slugify }}">
    <h1 class="content__title">{{ title }}</h1>
      {{ content | safe }}
  </article>
{% endblock %}
