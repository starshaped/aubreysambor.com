{% extends 'layouts/base.njk' %}

{% set introSummary %}
  {{ content | safe }}
{% endset %}

{% block content %}
  <main id="main-content" tabindex="-1">

    <div class="post-listing">

      {% include "layouts/partials/components/intro.njk" %}

      <h2 class="post-listing__heading">{{ postsHeading }}</h2>

      <ul class="post-listing__list">
        {%- for post in collections.homepagePosts -%}
          <li class="post-listing__item">
            <h3 class="post-listing__title">
              <a class="post-listing__link" href="{{ post.url }}" rel="bookmark">{{ post.data.title }}</a>
            </h3>
            <time class="post-listing__date" datetime="{{ post.date | htmlDateFilter }}">{{ post.date | formattedDateFilter }}</time>
          </li>
        {%- endfor -%}
      </ul>

      <a class="post-listing__see-all-link" href="/archives">
        See all posts
        <span aria-hidden="true">→</span>
      </a>
    </div>
  </main>
{% endblock %}

