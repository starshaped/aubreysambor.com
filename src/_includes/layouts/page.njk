{% extends 'layouts/base.njk' %}

{% block content %}
  <main id="main-content" tabindex="-1">
    <article class="page">
      <h1 class="page__title">{{ title }}</h1>
      {{ content | safe }}
    </article>
  </main>
{% endblock %}

{{ content | safe }}
